{
  "name": "vlab",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "nginx": "nginx -p . -c nginx.conf",
    "nginx:reload": "yarn nginx -s reload",
    "nginx:stop": "yarn nginx -s stop",
    "release": "yarn build && git add . && git commit -am 'release' && git push p master",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "luaparse": "^0.3.1",
    "prettier": "^2.6.2",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.3",
    "sass": "^1.52.1",
    "vite": "^2.9.9"
  }
}
